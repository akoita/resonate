# Resonate Cloud Deployment Configuration
# Copy to .env.deploy.<env> and fill in your values:
#   cp .env.deploy.example .env.deploy.dev
#
# Usage:  make deploy-all ENV=dev
#         make deploy-backend ENV=dev
#         make deploy-frontend ENV=dev

# ── GCP ──────────────────────────────────────────────────────────
REGISTRY=europe-west1-docker.pkg.dev/<your-gcp-project>/resonate-dev
GCP_REGION=europe-west1

# ── Frontend (NEXT_PUBLIC_* are baked in at Docker build time) ──
NEXT_PUBLIC_API_URL=https://resonate-dev-backend-XXXXX-ew.a.run.app
NEXT_PUBLIC_ZERODEV_PROJECT_ID=   # Get from https://dashboard.zerodev.app
NEXT_PUBLIC_CHAIN_ID=11155111
NEXT_PUBLIC_STEM_NFT_ADDRESS=
NEXT_PUBLIC_MARKETPLACE_ADDRESS=
