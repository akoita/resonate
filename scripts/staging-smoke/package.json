{
  "name": "resonate-staging-lifecycle-smoke",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Staging lifecycle smoke (#1392) — walks the full Shows money path against the real staging deployment.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "smoke": "node lifecycle-smoke.mjs",
    "smoke:dry-run": "node lifecycle-smoke.mjs --dry-run",
    "check": "node --check lifecycle-smoke.mjs"
  },
  "dependencies": {
    "viem": "^2.44.4"
  }
}
