# Gemini CLI / Antigravity custom command. Adapts the shared workflow (single source).
description = "Finish a Resonate issue — verify, test, security-route, document, and open/update the PR."
prompt = """
Follow the Resonate finish-issue workflow below. Run the steps in order, honour the
security routing in section 5, and never merge without an explicit request from the user.

@{.agents/skills/finish-issue/SKILL.md}
"""
