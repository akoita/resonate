import { privateMetadata } from "../../../lib/seo";

export const metadata = privateMetadata({ title: "AI DJ Quality" });

export default function AgentQualityAnalyticsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
