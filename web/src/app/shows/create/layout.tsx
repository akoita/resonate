import { privateMetadata } from "../../../lib/seo";

export const metadata = privateMetadata({ title: "Create show campaign" });

export default function CreateShowLayout({ children }: { children: React.ReactNode }) {
  return children;
}
