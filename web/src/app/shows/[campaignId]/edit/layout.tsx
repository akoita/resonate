import { privateMetadata } from "../../../../lib/seo";

export const metadata = privateMetadata({ title: "Edit show campaign" });

export default function EditShowLayout({ children }: { children: React.ReactNode }) {
  return children;
}
